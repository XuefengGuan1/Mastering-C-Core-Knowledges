#include <stdio.h>

int main()
{
    // Premitive datatypes
    int intV = 5;
    float floatV = 5.0;
    double doubleV = 5.00;
    char charV = 'a';

    // Type modifiers
    // Signed means that the number can be both positive and negative
    // Unsigned means that the number can only be positive
    // Short means a smaller version of int, on some systems, short == int
    // Long is a bigger version of int, and long long is the biggest of all

    signed int signedIntVariale = -5;
    unsigned int signedIntVariable = 5;
    short shortV = 5;
    long longV = 5;
    return 0;
}